import SearchBar from '../../components/SearchBar';
import ContactList from '../../containers/ContactList';

const Home = () => (
  <>
    <SearchBar />
    <ContactList />
  </>
);

export default Home;
